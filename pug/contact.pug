extends _main

block title
    | Contact |

block style
    link(rel='stylesheet', href='css/contact.css')

block content
    .hero
        h1 Contact Us
    .wrapper
        .creators.container
            h2.section-title Form
            input
            textarea(row='7')

    .contact.container
        .inner-container
            h3 Interested in a Demo?
            button.btn.btn-fill(type='button', onclick='contact();') CONTACT US
